/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com','www.refugee-action.org.uk'], // Tambahkan hostname di sini
}
};

export default nextConfig;
